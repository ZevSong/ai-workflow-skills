# DEMO-DOCS-02 · Worker · 故障排查文档

## 身份与输入

- Parent：`DEMO-DOCS`；Task：`DEMO-DOCS-02`；模式：`manual`，用户首次新建会话，修复续接本会话。
- 实施仓库与卡片仓库：`demo-handbook`；本卡根相对路径：`docs/task-packets/DEMO-DOCS/workers/DEMO-DOCS-02.md`。
- 将来使用独立分支 `docs/demo-docs-02`，工作目录为本任务独立 worktree 的仓库根 `.`。当前 fixture 无 Git 基线，隔离准备是已知执行前置条件，不是已完成事实。
- 必读：[README 原始请求](../../../../README.md)、[AGENTS.md](../../../../AGENTS.md)、[总卡](../packet.md)；独立 Reviewer：[DEMO-DOCS-02 审查卡](../reviews/DEMO-DOCS-02.md)。不依赖另一 Worker。
- 前置条件：输入和卡片可读、独立工作区就绪且用户发送启动提示词。生成本卡不满足执行授权。

## 结果、范围与步骤

计划创建 `demo-handbook` 根相对路径 `docs/troubleshooting.md`，用中文分别解释三种问题的可观察现象和下一步。允许该文档及 `docs/task-packets/DEMO-DOCS/reports/DEMO-DOCS-02-handoff.md`（计划创建）的本地写入与本任务修复；不得修改另一份文档、总卡、状态或审查报告。

先核对分支和既有改动，保留用户工作，再组织三种情况：卡片缺失时说明材料无法读取，核对仓库标识与根相对路径并取得完整任务包；需求不明确时说明缺少行为、范围或验收决定，列出缺少什么、影响什么、需要决定什么，等待补齐再拆分；自动调度工具缺失时说明所选自动载体无法创建/续接/跟踪会话，如实报告能力缺口并提供人工提示词，由用户决定调整模式。当前人工模式本身不依赖自动工具。措辞和版式由 Worker 决定。

非目标：安装或自动修复工具、执行调度、产品代码、发布；默认不推送、不创建远端资源、不合并。禁止检查 SHA，也不使用其他哈希绕过该限制。不自行启动 Reviewer 或其他会话。

## 阅读验收

以下均为未来计划验证，本次生成时为 UNRUN；没有现有测试命令。验证工作目录为本任务独立 worktree 的 `demo-handbook` 根 `.`，工具为文本阅读器。

| 编号 / 需求 | 输入与操作 | 期望结果 | 方法 | 证据 |
| --- | --- | --- | --- | --- |
| A3 / R2 | 打开 `docs/troubleshooting.md`，阅读卡片缺失条目 | 有无法读取材料的可观察现象；下一步是核对仓库/相对路径并获取缺失材料，不让作者聊天替代任务卡 | 阅读现象与下一步两处文字 | 报告记录标题或行号和观察 |
| A4 / R2 | 阅读需求不明确条目 | 说明缺少行为、范围或验收决定；下一步列明缺口、影响、所需决定，补齐前不正式拆分 | 对照三类信息与等待边界 | 报告逐项记录位置和判断 |
| A5 / R2 | 阅读自动调度工具缺失条目 | 指出相关能力不可用的现象；报告缺口并给人工入口，用户决定切换；不承诺安装或静默降级，并说明人工模式不依赖自动工具 | 阅读能力缺口、下一步和模式边界 | 报告记录三处位置及判断 |
| B1 / R3 | 阅读全文并查看本任务实际文件差异 | 中文正文，三种情形均有现象与下一步；无实际安装/调度/发布或虚构运行结果，仅两个允许文件有本任务写入 | 全文阅读及逐文件差异检查，不计算哈希 | 报告列文件清单、边界核对及未执行事项 |

## 交付、受阻与修复

交接报告属于仓库 `demo-handbook`，位置为 `docs/task-packets/DEMO-DOCS/reports/DEMO-DOCS-02-handoff.md`（计划创建）。记录分支名、交付批次编号、本次完整文档内容副本、修改文件、A3/A4/A5/B1 的实际阅读操作与观察、未解决事项。完整内容副本供 Reviewer 对照当前文件，不依赖提交哈希；后续修改提升批次并更新副本。

如果卡片缺失、目标文件已由他人新增或隔离条件不具备，报告现象和所缺材料，停止冲突写入。范围内失败由本 Worker 修复；同一问题反复出现且无新证据或可行办法时报告阻塞。回滚只处理本任务新增内容，不覆盖已有改动。

交付后停止，等待用户把该批次文件和报告交给独立 Reviewer。审查发现问题后，用户续接本会话；只修复本任务问题，重新阅读受影响项目并更新交付批次，等待原 Reviewer 复审，不自行宣布审查通过。
