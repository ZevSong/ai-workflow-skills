# DEMO-DOCS-01 · Worker · 快速入门文档

## 身份与输入

- Parent：`DEMO-DOCS`；Task：`DEMO-DOCS-01`；模式：`manual`，用户首次新建会话，修复续接本会话。
- 实施仓库与卡片仓库：`demo-handbook`；本卡根相对路径：`docs/task-packets/DEMO-DOCS/workers/DEMO-DOCS-01.md`。
- 将来使用独立分支 `docs/demo-docs-01`，工作目录为本任务独立 worktree 的仓库根 `.`。当前 fixture 无 Git 基线，隔离准备是已知执行前置条件，不是已完成事实。
- 必读：[README 原始请求](../../../../README.md)、[AGENTS.md](../../../../AGENTS.md)、[总卡](../packet.md)；独立 Reviewer：[DEMO-DOCS-01 审查卡](../reviews/DEMO-DOCS-01.md)。不依赖另一 Worker。
- 前置条件：输入和卡片可读、独立工作区就绪且用户发送启动提示词。生成本卡不满足执行授权。

## 结果、范围与步骤

计划创建 `demo-handbook` 根相对路径 `docs/quickstart.md`，以中文清晰描述四步和生成边界。允许该文档及 `docs/task-packets/DEMO-DOCS/reports/DEMO-DOCS-01-handoff.md`（计划创建）的本地写入与本任务修复；不得修改另一份文档、总卡、状态或审查报告。

先核对分支和既有改动，保留用户工作，再按四步组织正文：获取包含 SKILL.md 及其依赖资源的完整目录；根据使用宿主的说明选择安装位置；明确使用 `$plan-agent-tasks` 显式调用；查看任务卡、流程图和具体会话提示词。明确“生成计划不会启动业务，后续由用户按人工模式启动会话”。段落措辞由 Worker 决定，不需要安装或运行任何工具来证明文档内容。

非目标：实际安装、实际调度、产品代码、发布；默认不推送、不创建远端资源、不合并。禁止检查 SHA，也不使用其他哈希绕过该限制。不自行启动 Reviewer 或其他会话。

## 阅读验收

以下均为未来计划验证，本次生成时为 UNRUN；没有现有测试命令。验证工作目录为本任务独立 worktree 的 `demo-handbook` 根 `.`，工具为文本阅读器。

| 编号 / 需求 | 输入与操作 | 期望结果 | 方法 | 证据 |
| --- | --- | --- | --- | --- |
| A1 / R1 | 打开 `docs/quickstart.md`，逐段标出获取目录、按宿主安装、显式调用、查看结果的位置 | 四步齐全、顺序清楚；获取说明包含依赖资源；安装以宿主说明为准；调用名称明确；结果包含卡片、图和提示词 | 依次阅读四段，逐项对照原始请求 | 交接报告列出四段行号或标题及观察 |
| A2 / R1 | 找到生成行为与后续执行的说明，阅读相邻上下文 | 明确只生成计划不会启动业务；人工启动后才执行，不声称自动安装或调度 | 阅读边界句及其上下文 | 报告记录位置、原文短引和判断 |
| B1 / R3 | 阅读全部正文并查看本任务实际文件差异 | 中文正文，无真实安装/调度/发布行为；仅两个允许文件有本任务写入，无虚构运行结果 | 全文阅读及逐文件差异检查，不计算哈希 | 报告列文件清单、边界核对及未执行事项 |

## 交付、受阻与修复

交接报告属于仓库 `demo-handbook`，位置为 `docs/task-packets/DEMO-DOCS/reports/DEMO-DOCS-01-handoff.md`（计划创建）。记录分支名、交付批次编号、本次完整文档内容副本、修改文件、A1/A2/B1 的实际阅读操作与观察、未解决事项。完整内容副本用于 Reviewer 对照当前文件，不依赖提交哈希；后续修改提升批次并更新副本。

如果卡片缺失、目标文件已由他人新增或隔离条件不具备，报告现象和所缺材料，停止冲突写入。范围内失败由本 Worker 修复；同一问题反复出现且无新证据或可行办法时报告阻塞。回滚只处理本任务新增内容，不覆盖已有改动。

交付后停止，等待用户将该批次文件和报告交给独立 Reviewer。审查发现问题后，用户续接本会话；只修复本任务问题，重新阅读受影响项目并更新交付批次，等待原 Reviewer 复审，不自行宣布审查通过。
